@model ArtistSearch.Presentation.Model.ArtistSearchModel

@{
    ViewData["Title"] = "Home Page";
}

@using (Html.BeginForm())
{    
    <div class="row">
        <div>
            <h2>Searh your artist:</h2>
            @Html.TextBoxFor(m => m.Name, new { placeholder = "Artist name..." })            
            <button class="btn-primary " value="Find"> 
                    <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
            </button>            
        </div>
    </div>

    @if (Model.Artist != null)
    {
        <h3>Artist information</h3>        
        <div class="row">
            <div>                
                <image src="@Model.Artist.Image_Url"></image>
            </div>
            <br />
        </div>
        <div class="row">
            <div>
                @Html.Label("Name: ")
                @Model.Artist.Name
            </div>
        </div>
        <div class="row">
            <div>
                @Html.Label("Facebook URL: ")
                @Model.Artist.Facebook_Page_Url
            </div>
        </div>

        <h3>Events</h3>

        foreach (var artistEvent in Model.Artist.Events)
        {
            <div class="row">
                <div>
                    @Html.Label("Venue: ")
                    @artistEvent.Venue.Name
                </div>
            </div>
            <div class="row">
                <div>
                    @Html.Label("City: ")
                    @artistEvent.Venue.City
                </div>
            </div>
            <div class="row">
                <div>
                    @Html.Label("Country: ")
                    @artistEvent.Venue.Country
                </div>
            </div>
            <div class="row">
                <div>
                    @Html.Label("Date: ")
                    @artistEvent.DateTime
                </div>
            </div>
            <hr />
        }
    }
}
